Type casting is an explicit conversion where the programmer manually specifies the target data type using a casting 

operator (e.g., int (x)), while type conversion is typically an implicit process where the compiler automatically 

changes the data type without programmer intervention.